{
  "$comment": "Canonical MCP install config - the SINGLE source all generated install snippets, deeplinks, and README install blocks derive from. Edit this file, then run install/generate-install-links.ps1 to regenerate install/generated/ and the README install-buttons block. CI fails if they drift. The package VERSION is intentionally absent here - it is read from build/dependencies.props (<GroupDocsMetadataMcp>) at generation time so there is exactly one version source of truth.",
  "name": "groupdocs-metadata",
  "displayName": "GroupDocs Metadata",
  "packageId": "GroupDocs.Metadata.Mcp",
  "toolCommand": "groupdocs-metadata-mcp",
  "registryName": "io.github.groupdocs-metadata/groupdocs-metadata-mcp",
  "ghcrImage": "ghcr.io/groupdocs-metadata/metadata-net-mcp",
  "dockerHubImage": "groupdocs/metadata-net-mcp",
  "testsRepo": "groupdocs-metadata/GroupDocs.Metadata.Mcp.Tests",
  "command": "dnx",
  "args": ["GroupDocs.Metadata.Mcp", "--yes"],
  "env": {
    "GROUPDOCS_MCP_STORAGE_PATH": "${input:storage_path}",
    "GROUPDOCS_LICENSE_PATH": "${input:license_path}"
  },
  "inputs": [
    {
      "id": "storage_path",
      "description": "Base folder for input and output files",
      "required": true
    },
    {
      "id": "license_path",
      "description": "Path to GroupDocs license file (optional - evaluation mode if empty)",
      "required": false
    }
  ]
}
